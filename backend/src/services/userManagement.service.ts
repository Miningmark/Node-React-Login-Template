export class UserManagementService {
    constructor() {}

    async addUser() {
        let jsonResponse: Record<string, any> = { message: "Benutzer erfolreich hinzugefügt" };

        return jsonResponse;
    }
}
